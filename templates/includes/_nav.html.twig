<nav>
  <div class="topnav" id="myTopnav">
    {# <div class="navbar-left"> #}
      <a aria-current="page" href="{{path('app_default_show_home')}}">Accueil</a>
      <a href="{{path('app_default_show_starter')}}">Entrées</a>
      <a href="{{path('app_default_show_dish')}}">Plats</a>
      <a href="{{path('app_default_show_dessert')}}">Desserts</a>
      <a href="{{path('app_default_show_drink')}}">Boissons</a>
    {# </div> #}
    {# <div class="navbar-right"> #}
              {# ADMIN #}
      {% if is_granted("ROLE_ADMIN") %}
      <a href="{{path('app_admin_show_dashboard')}}">Dashboard</a>
      <a href="{{path('app_admin_food_list')}}">Gérer la carte</a>
      <a href="{{path('app_admin_user_list')}}">Gérer les utilisateurs</a>
      <a href="{{path('app_admin_booking_list')}}">Gérer les réservations</a>
      <a href="{{path('app_admin_comment_list')}}">Gérer les commentaires</a>
      {% endif %}
  
          {# USER #}
      {% if is_granted("ROLE_USER") and not is_granted("ROLE_ADMIN") %}
      <a href="{{path('app_user_show_account')}}">Mon compte</a>
      {% endif %}
  
  
  
          {# CONNEXION #}
      {% if not app.user %}

          <a  href="{{ path('app_login') }}" title="Connectez-vous">Connexion</a>
  
           <a href="{{ path('app_register') }}" title="Inscrivez-vous">Inscription</a>
  
      {% else %}
              
          <a href="{{ path('app_logout') }}" title="Déconnexion">Déconnexion</i></a>
  
      {% endif %}

          <a href="javascript:void(0);" class="icon" onclick="myFunction()"><i class="fa fa-bars"></i></a>
    

  </div>


</nav>

{# VERSION BOOTSTRAP #}
{# <nav class="navbar navbar-expand-lg bg-light">
  <div class="container-fluid">
    <a class="navbar-brand" href="#">Veggie Noodle</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
      <div class="navbar-nav">
        <a class="nav-link active" aria-current="page" href="{{path('app_default_show_home')}}">Accueil</a>
        <a class="nav-link" href="{{path('app_default_show_starter')}}">Entrées</a>
        <a class="nav-link" href="{{path('app_default_show_dish')}}">Plats</a>
        <a class="nav-link" href="{{path('app_default_show_dessert')}}">Desserts</a>
        <a class="nav-link" href="{{path('app_default_show_drink')}}">Boissons</a>
        <a class="nav-link" href="#">Notre équipe</a>

    
        {% if is_granted("ROLE_ADMIN") %}
        <a class="nav-link" href="{{path('app_admin_show_dashboard')}}">Dashboard</a>
        <a class="nav-link" href="{{path('app_admin_food_list')}}">Gérer la carte</a>
        <a class="nav-link" href="{{path('app_admin_user_list')}}">Gérer les utilisateurs</a>
        <a class="nav-link" href="{{path('app_admin_booking_list')}}">Gérer les réservations</a>
        {% endif %}


        {% if is_granted("ROLE_USER") and not is_granted("ROLE_ADMIN") %}
        <a class="nav-link" href="{{path('app_user_show_account')}}">Mon compte</a>
        {% endif %}



   
            {% if not app.user %}

                
                <a href="{{ path('app_login') }}" title="Connectez-vous" class="nav-link">Connexion</a>

                
                <a href="{{ path('app_register') }}" title="Inscrivez-vous" class="nav-link">Inscription</a>

            {% else %}
            
                <a href="{{ path('app_logout') }}" title="Déconnexion" class="nav-link">Déconnexion</i></a>

            {% endif %}


      </div>
    </div>
  </div>
</nav> #}